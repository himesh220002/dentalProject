🚀 STAGING ENVIRONMENT SETUP (testbranch01)

To make everything work (Google Login, Mail, Database), please add these secrets.

================================================================================
PART 1: GITHUB ENVIRONMENT SECRETS (Go to Settings > Environments > Staging)
================================================================================

--- 🔹 CORE DEPLOYMENT ---
VERCEL_TOKEN                 -> Your Vercel account token
VERCEL_ORG_ID                -> Your Vercel team ID
STAGING_BACKEND_URL          -> https://your-render-staging-app.onrender.com
RENDER_STAGING_DEPLOY_HOOK    -> The URL from Render Settings > Deploy Hook
STAGING_FRONTEND_URL         -> Your Vercel Preview URL (e.g. https://xxx-git-testbranch01-xxx.vercel.app)

--- 🔹 AUTHENTICATION (NextAuth/Google) ---
GOOGLE_CLIENT_ID             -> (Use your existing one or unique staging one)
GOOGLE_CLIENT_SECRET         -> (Use your existing one or unique staging one)
NEXTAUTH_SECRET              -> (Create a NEW random string for staging)

================================================================================
PART 2: RENDER DASHBOARD ENV (Go to Render > Staging Service > Environment)
================================================================================

⚠️ CRITICAL: FIX RENDER TYPO
In Render Dashboard > Settings:
- Build Command: `npm install`
- Start Command: `npm start` (Make sure it is NOT `num run dev`)

--- 🔹 DATABASE ---
MONGO_URI                    -> mongodb+srv://.../dentalTestDB

--- 🔹 URLS & SECURITY ---
FRONTEND_URL                 -> (Your Vercel Preview URL)
JWT_SECRET                   -> (Create a NEW random string for staging)

--- 🔹 MAILING ---
GMAIL_USER                   -> satyamhimesh@gmail.com
GMAIL_APP_PASS               -> swyhxhuqcisjnivk
MAILGUN_API_KEY              -> (Use your existing one)
MAILGUN_DOMAIN               -> (Use your existing one)

================================================================================
🛡️ DATA ISOLATION (How to keep Staging separate from Production)
================================================================================

1. In Render Dashboard, make sure you have TWO separate services.
2. The Staging service must be connected to the `testbranch01` branch.
3. In the Staging service's "Environment" tab:
   - MONGO_URI must end in `/dentalTestDB` (NOT `/dentalDB`)
   - JWT_SECRET should be different from Production.
4. This ensures that any test appointments or patients you create in STAGING 
   will NEVER appear on your main live website.

================================================================================
💡 PRO TIP: 
You only need to add these ONCE. Any future push to testbranch01 will 
automatically use these values and deploy everything 100% Green!
================================================================================